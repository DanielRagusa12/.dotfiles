#!/bin/sh
set -eu

# Re-run when the pinned dependency graph changes.
# package-lock checksum: {{ include "dot_pi/agent/extensions/opencode-web/package-lock.json" | sha256sum }}

extension_dir={{ (joinPath .chezmoi.sourceDir "dot_pi/agent/extensions/opencode-web") | quote }}
cd "$extension_dir"
npm ci --ignore-scripts
