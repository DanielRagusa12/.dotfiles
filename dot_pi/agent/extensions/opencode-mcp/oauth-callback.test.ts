import { afterEach, describe, expect, test } from "bun:test"; import { ensureCallback, stopCallbacks, waitForCallback } from "./oauth-callback.ts";
afterEach(stopCallbacks); describe("OAuth callback",()=>{test("validates state and escapes provider errors",async()=>{const port=22000+Math.floor(Math.random()*1000);await ensureCallback(`http://127.0.0.1:${port}/cb`);const pending=waitForCallback("good","server",1000).then(() => "resolved", (error) => error as Error);const bad=await fetch(`http://127.0.0.1:${port}/cb?state=bad&code=x`);expect(bad.status).toBe(400);const response=await fetch(`http://127.0.0.1:${port}/cb?state=good&error=%3Cscript%3E`);expect(await response.text()).not.toContain("<script><script>");const result=await pending;expect(result).toBeInstanceOf(Error);expect((result as Error).message).toContain("<script>");});});
